.DS_Store
node_modules
dist
.turbo
.next
.vercel
.vitest
.lighthouseci
coverage

# Playwright
test-results
playwright-report
.playwright
*.tsbuildinfo
*.log
*.local
.env
.env.*
!.env.example

# Local Claude Code config
.claude/

# Superpowers brainstorming (local mockups, not committed)
.superpowers/

# Playwright MCP page snapshots — debugging debris, never a build input.
.playwright-mcp/
playwright.local.config.ts
