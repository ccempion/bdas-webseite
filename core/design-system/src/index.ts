/**
 * Shared design system — tokens, Tailwind preset, and primitive components.
 * Sprint 1 fills this in with Button, Input, Form, Card, Alert. The WordPress
 * theme also consumes this preset so the visual transition is invisible.
 */

export const DESIGN_SYSTEM_VERSION = "0.0.0";
